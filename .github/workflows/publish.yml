name: Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  publish:
    uses: UtopikCode/github-actions/.github/workflows/go-publish.yml@main
    with:
      go-version: '1.23'
      make-target: 'ci'
